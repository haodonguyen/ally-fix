# Dependencies
node_modules/
.pnpm-store/

# Environment — NEVER commit real secrets, only .env.example is tracked
.env
.env.*
!.env.example

# Model comparison config: names env vars, but it is per-machine and records
# real prices. The example is tracked; the working copy is not.
apps/worker/eval/models.json

# Build output
dist/
build/
.next/
out/
*.tsbuildinfo

# Drizzle generated migrations meta (keep SQL, ignore local snapshots if desired)
# .drizzle/

# Playwright
/test-results/
/playwright-report/
/playwright/.cache/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Local-only notes (kept on disk, not committed)
CLAUDE.md
CLAUDE.local.md

# Coverage
coverage/
